<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Misc on Flock137</title>
    <link>http://localhost:1313/tags/misc/</link>
    <description>Recent content in Misc on Flock137</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <lastBuildDate>Wed, 03 Sep 2025 00:00:00 +0700</lastBuildDate><atom:link href="http://localhost:1313/tags/misc/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Merging multiple singular .ics files into one in Linux</title>
      <link>http://localhost:1313/posts/calendar/</link>
      <pubDate>Wed, 03 Sep 2025 00:00:00 +0700</pubDate>
      
      <guid>http://localhost:1313/posts/calendar/</guid>
      <description>Step 1: Gather your individual .ics files in a folder. Open the terminal in the said folder. Type this in your console.
# Comhbine all .ics to a new text file cat *.ics &amp;gt; temp.txt Step 2: If you check with cat temp.txt you can see there is this weird string that you need to get rid off: END:VCALENDARBEGIN:VCALENDAR. Use cat temp.txt | grep -v DARBEG &amp;gt; temp1.txt
Step 3: For my case, I also need to:</description>
      <content:encoded><![CDATA[<p>Step 1: Gather your individual .ics files in a folder. Open the terminal in the said folder. Type this in your console.</p>
<pre tabindex="0"><code># Comhbine all .ics to a new text file
cat *.ics &gt; temp.txt 
</code></pre><p>Step 2: If you check with <code>cat temp.txt</code> you can see there is this weird string that you need to get rid off: <code>END:VCALENDARBEGIN:VCALENDAR</code>. Use <code>cat temp.txt | grep -v DARBEG &gt; temp1.txt</code></p>
<p>Step 3: For my case, I also need to:</p>
<ul>
<li>Adjust so that METHOD, PRODID and VERSION only appeared once</li>
</ul>
<pre tabindex="0"><code># Concatenate temp1.txt; -v means &#39;inverse grep&#39;
cat temp1.txt | grep -v -e METHOD -e PRODID -e VERSION &gt; temp2.txt 
</code></pre><p>Note: You need to manually put back your METHOD, PUBLISH and VERSION back manually after using the command above. They should be located after <code>BEGIN: VCALENDAR</code> and right before <code>BEGIN:VEVENT</code></p>
<ul>
<li>Fix the UID, since they can not be the same. Its syntax should be something like this (rid of the quotes): &ldquo;<a href="mailto:mech-a314@synthesis.com">mech-a314@synthesis.com</a>&rdquo;, &ldquo;MECH&rdquo;, &ldquo;1337&rdquo;, etc. If you see something like &lsquo;^M&rsquo; in your text editor, ignore them</li>
</ul>
<p>Step 4: And finally,</p>
<pre tabindex="0"><code># Export text to iCalendar file. 
cat temp2.txt &gt; calendar.ics
</code></pre><p>Congrats, now all your calendar files are in a file files now.
Please don&rsquo;t delete your individual .ics files, yet. You might need them later on, they don&rsquo;t take much space.</p>
]]></content:encoded>
    </item>
    
  </channel>
</rss>
